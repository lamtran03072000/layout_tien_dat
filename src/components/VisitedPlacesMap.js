import React, { useEffect } from 'react';

function VisitedPlacesMap() {
  // Bạn cần có một phần tử với id="chartdiv" để script nhắm đến
  return <div id="chartdiv" style={{ width: '100%', height: '600px' }} />;
}

export default VisitedPlacesMap;
